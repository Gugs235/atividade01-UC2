while True:
    #o usuario coloca a quantidade de vezes que vai aumentar
    x = float(input("Digite a quantidade que vai aumentar a experiencia: "))
    #o usuario coloca a quantidade que normalmente da
    m = float(input("Digite a quantidade que normalmente da: "))
    if x == 0 and m == 0: #se o x e m for igual a 0 o programa para
         break
    else:
        #faz a multiplicação dos valores